import ResetPasswordClient from "@/components/auth/ForgotPasswordClient";

export const metadata = { title: "Reset Password | Zaro Bakehouse" };

export default function ResetPasswordPage() {
  return <ResetPasswordClient />;
}